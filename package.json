{
  "name": "morphly-api",
  "version": "1.2.33",
  "private": true,
  "type": "module",
  "dependencies": {
    "@supabase/supabase-js": "^2.100.1",
    "concurrently": "^9.2.1"
  },
  "scripts": {
    "build:app": "npm install --prefix app && npm run build:app --prefix app",
    "cleanup:stale-sessions": "npm run cleanup:stale-sessions --prefix app"
  }
}
